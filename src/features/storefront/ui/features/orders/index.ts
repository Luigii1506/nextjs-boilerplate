/**
 * 📦 ORDERS FEATURE (UI Components)
 * ==================================
 *
 * UI components for orders display
 */

export { OrderDetailsModal } from "./OrderDetailsModal";

// Re-export types from orders feature for convenience
export type { Order, OrderSummary } from "@/features/storefront/orders";
